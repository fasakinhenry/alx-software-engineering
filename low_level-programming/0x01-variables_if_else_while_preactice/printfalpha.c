#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entty point of the program
 *
 * Return: Always 0(success)
 */

int main(void)
{
	/* This is to illustrate that char is an int */
	char alpha = 'a';
	printf("%d\n", alpha);

	return (0);
}
